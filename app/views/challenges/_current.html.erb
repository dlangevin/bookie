<h3> Your challenges</h3>
<% 
  # Challenge.where(:started_by => current_user.id).each do |c| 
  @current_users_challenges.each do |c|
%>
<div id = "current">
  You bet <b><%= c.wager %></b> for someone to <b><%= c.challenge_name %></b> by 
    <b><%= c.due_date %></b></br>
  
	<% if c.hookups.present? %>
	  <%= # Hookup.where(:challenge_id => c.id).each do |h| 
	    c.hookups.collect{|h| h.user.email}.join(", ")
    %>
  <% else %>
    nobody has yet
  <% end %>

	accepted this challenge.</br>
	
	<% if c.completer? %>
	  Finished by <b><%= c.completer.email %> </b> 
	    on <b><%= c.complete_date %></b>
	<% end %>		
</div>
</br>
<% end %>